<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/widget_quick_add"
    android:minWidth="120dp"
    android:minHeight="50dp"
    android:resizeMode="none"
    android:updatePeriodMillis="0" 
    android:widgetCategory="home_screen"
    android:label="@string/widget_quick_add_label"
    android:description="@string/widget_quick_add_desc"
    android:previewLayout="@layout/widget_quick_add"
    android:previewImage="@drawable/preview_widget_quick_add" />
<!-- Update period 0 because it's just a launcher button, doesn't need data refresh -->
